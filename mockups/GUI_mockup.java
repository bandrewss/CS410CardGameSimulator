public class GUI extends JFrame {

	...

	public GUI(String name, Client c) {
		client = c;
		frame = new JFrame();

		// build grid panel

		cardButtons = new JButton[17];

		for (/*all of the buttons*/) {
			// build button
			// assign to the grid
			// add action listener
		}

		// build scroll pane

		// add scoll pane to NORTH
		// add grid pane to CENTER
	}

	...

}
